//
// Created by Wring on 2/12/2024.
//

#include "PressureSensorDriver.h"

void PressureSensorDriver::setup() {
    //initialize sensor
}

void PressureSensorDriver::loop() {
    //routine read sensor values
    //If you need to wait between loops, instead of using wait(), if(lastTime - currentTime < waitTime) return;
}
